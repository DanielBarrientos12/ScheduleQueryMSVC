spring:
  application:
    name: schedule-query-msvc

  datasource:
    url: ${JDBC_DATABASE_URL:jdbc:postgresql://postgres-db:5432/vet}
    username: ${JDBC_DATABASE_USERNAME:postgres}
    password: ${JDBC_DATABASE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false

  rabbitmq:
    host: ${RABBITMQ_HOST:rabbitmq}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}
    listener:
      simple:
        retry:
          enabled: true
          initial-interval: 2000     # 2s
          multiplier: 2.0            # backoff exponencial
          max-interval: 30000        # limite 30s entre reintentos
          max-attempts: 5            # 5 intentos en total (1 intento + 4 reintentos)
        default-requeue-rejected: false

  jackson:
    serialization:
      write-dates-as-timestamps: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

server:
  port: ${PORT:8082}

app:
  exchange: vetcore.events
  queue: schedule.query.queue
  dlq: schedule.query.dlq
  routing-keys:
    - appointment.created
    - appointment.rescheduled
    - appointment.cancelled
    - appointment.attended
  timezone: "America/Bogota"
